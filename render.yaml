services:
  - type: web
    name: qr-manager
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: BASE_URL
        value: https://qr-manager-jbqt.onrender.com
    healthCheckPath: /health
